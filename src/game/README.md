# game/

All gameplay logic sits under `game/`. Right now `core/` hosts the console engine and supporting systems, but the namespace is intentionally short so we can add future gameplay modes (e.g., network or physics experiments) alongside it.
