# geom/

Utility math classes. Currently only `Vector2` lives here, but keeping geometry helpers isolated makes it easier to add more advanced math or physics primitives later without mixing them into gameplay classes.
