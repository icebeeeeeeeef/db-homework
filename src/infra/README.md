# infra/

Infrastructure utilities shared across packages. `ToolPaths` centralizes filesystem locations such as the `tools/code-stats/code_stats` binary so both CLI and GUI layers refer to the same executable.
