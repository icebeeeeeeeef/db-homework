# tools/

Native helpers and binary artifacts:

- `code-stats/` – the C++ code statistics tool that feeds the GUI charts
- `toukei.dll` – legacy DLL kept for reference/backwards compatibility

Each subdirectory documents its own build/run instructions.
